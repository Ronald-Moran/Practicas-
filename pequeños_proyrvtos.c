#include <stdio.h>
#include <string.h>

int main() {
 
char texto[] = "hola";
int ant = strlen(texto);
int contar = 0;

for(int i = ant - 1; i < sizeof(texto); i++) {
       
}

    return 0;
}